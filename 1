#include <iostream>
#include <Windows.h>//для отображения кириллицы
using namespace std;
int main() {
    int a;
    cin>>a;
    SetConsoleOutputCP(CP_UTF8);//для отображения кириллицы
    if ((a>=100) and (a<1000)){
        cout<<"Трехзначное";
    }
    else{
        cout<<"Не трехзначное";
    }
}
